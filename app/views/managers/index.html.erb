<p id="notice"><%= notice %></p>

<section class="header">
  <div class="d-flex justify-content-between">
    <h1 class="col-md-3">Managers</h1>
    <section class="search-form col-md-9">
      <%= form_for :managers, method: :get do |f| %>
        <div class="input-group input-group-lg">
          <%= label_tag :keywords, nil, class: "sr-only" %>
          <%= text_field_tag :keywords, nil,
                placeholder: "Search managers by section or name",
                class: "form-control input-lg" %>
          </span>
        </div>
      <% end %>
    </section>
  </div>
</section>

<%= render partial: 'top_pager' %>

<section class="manager-panels">
  <% @managers.each do |manager| %>
      <%= render manager %>
  <% end %>
</section>

<%= render partial: 'bottom_pager' %>
