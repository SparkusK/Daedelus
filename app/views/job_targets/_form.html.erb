<%= form_with(model: job_target, local: true) do |form| %>
  <% if job_target.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_target.errors.count, "error") %> prohibited this job_target from being saved:</h2>

      <ul>
      <% job_target.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :target_date %>
    <%= form.date_select :target_date, id: :job_target_target_date %>
  </div>

  <div class="field">
    <%= form.label :invoice_number %>
    <%= form.text_field :invoice_number, id: :job_target_invoice_number %>
  </div>

  <div class="field">
    <%= form.label :remarks %>
    <%= form.text_field :remarks, id: :job_target_remarks %>
  </div>

  <div class="field">
    <%= form.label :details %>
    <%= form.text_field :details, id: :job_target_details %>
  </div>

  <div class="field">
    <%= form.label :target_amount %>
    <%= form.text_field :target_amount, id: :job_target_target_amount %>
  </div>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.text_field :section_id, id: :job_target_section_id %>
  </div>

  <div class="field">
    <%= form.label :job_id %>
    <%= form.text_field :job_id, id: :job_target_job_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
