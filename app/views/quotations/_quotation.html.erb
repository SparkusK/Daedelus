<div class="card" id="<%= quotation.id %>">
  <div class="card-header">
    <div class="d-flex justify-content-between">
      <h4>
        <%= quotation.code %>
      </h4>
      <h5>
      </h5>
    </div>
  </div>
  <div class="card-body">
    <div class="row">
      Stub. More stuff to go here
    </div>
    <div class="row">
      <div class="col-md-10"></div>
      <div class="d-flex col-md-2">
        <%= button_to 'Edit', edit_quotation_path(quotation), class: "btn btn-primary" %>
        <%= button_to 'Remove', quotation, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>

<br>
